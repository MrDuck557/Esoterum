package esoterum.content;

import arc.graphics.g2d.*;
import mindustry.entities.*;

public class EsoFx{
    public static Effect

    manualLand = new Effect(60f, e -> { //TODO this is a placeholder, Goob replace this with your effect later
        Fill.circle(e.x, e.y, 2f);
    });
}
